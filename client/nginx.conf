server {
    listen 8080;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: serve index.html for any unmatched route (React Router).
    location / {
        try_files $uri $uri/ /index.html;
    }
}
