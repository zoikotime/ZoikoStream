// DashboardCard is the design system's StatsCard. Kept as a re-export so existing
// dashboard imports keep working while the implementation lives in one place.
export { default } from "../../ui/StatsCard";
